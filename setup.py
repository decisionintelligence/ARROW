from setuptools import setup, find_packages

setup(
    name='GlobWeather',
    version='0.0.1',  # Your package version
    description='Global Weather forecasting',
    packages=find_packages(),
    install_requires=[]
)